// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // davidrtriana.github.io is a user site, so it serves from the domain root (no `base` needed).
  site: "https://davidrtriana.github.io",
});
